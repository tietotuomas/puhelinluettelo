{
  "name": "puhelinluettelo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "start": "node index.js",
    "build:ui": "rm -rf build && cd ../Fullstack2021/osa2/puhelinluettelo/ && npm run build --prod && cp -r build ../../../puhelinluettelo",
    "deploy": "git push heroku main",
    "deploy:full": "npm run build:ui && git add . && git commit -m Deploy full && git push && npm run deploy",    
    "logs:prod": "heroku logs --tail"
  },
  "author": "tietotuomas",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
